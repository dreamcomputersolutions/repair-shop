[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# This redirects the app's call to the hidden server function
[[redirects]]
  from = "/api/send-email"
  to = "/.netlify/functions/send-email"
  status = 200